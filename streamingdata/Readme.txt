// step1: install python libraries using following command
python -m pip install requests plotly dash jupyter-dash

// run the python .ipynb file

// a webui will open on the following URL http://127.0.0.1:8050/ open it on browser, application will work ... Also it can be seen inside juputernotebook as well

